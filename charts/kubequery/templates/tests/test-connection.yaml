apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubequery.fullname" . }}-test-connection"
  labels:
    {{- include "kubequery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubequery.fullname" . }}:{{ .Values.test.service.port }}']
  restartPolicy: Never
